# conduit.testing (not started)

This package will hold the acceptance-test harness (`AcceptanceTestDriver`
Protocol + `ConfigurableAcceptanceTestDriver`) per
`docs/design/20260707-python-connector-sdk.md` §3 and the v0.19 workstream's
Lane C. **Lane C depends on Lane B (B2 Source/Destination ABCs, B5 the B1
ack/nack fix)**, neither of which exists yet as of this scaffold — do not
import from this package expecting anything to work.
